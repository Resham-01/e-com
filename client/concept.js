/*
npm, npx
commands:-
    1. npx create-react-app appName
    if errors occure:-
        npm uninstall -g create-react-app

- progressive web app


components:
    component is basic bulding block if react
    used to make a UI
    component must return single html node

Two way of making a component
    1. stateful component (class component)
    2. stateless component (functional component)

16.8 => hooks => functional component can have state

Two type of component:
    1. functional component
    2. class component

component data:
    state => data within a component
    props => incoming data for a component
*/

/**
 * git clone
 * git status
 * git add .
 * git commit
 * git fetch
 * git push
 * git pull
 * git log
 */
